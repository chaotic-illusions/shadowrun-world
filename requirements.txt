# Pinned for reproducible Docker builds. Bump deliberately.
# anthropic / aiofiles / pytest-asyncio were not in the local venv at review time, so
# they are bounded (block surprise majors) rather than pinned to an exact version.
fastapi==0.135.2
uvicorn[standard]==0.42.0
sqlalchemy==2.0.48
pydantic==2.12.5
python-dotenv==1.2.2
aiofiles>=23.0.0,<25.0.0
aiosqlite==0.22.1
anthropic>=0.40.0,<1.0.0
keyring==25.7.0
alembic==1.18.4
pytest==9.0.3
pytest-asyncio>=0.24.0,<1.0.0
httpx==0.28.1
