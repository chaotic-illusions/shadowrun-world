# Dev / tooling dependencies -- NOT required to run the app.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

playwright>=1.40,<2  # tests/test_character_builder_dom.py -- headless DOM smoke test (optional)
